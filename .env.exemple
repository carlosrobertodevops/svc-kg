
# Connection string do Postgres do Supabase (role dedicada):
# create role svc_kg login password 'SENHA_FORTE';
# grant usage on schema public to svc_kg;
# grant execute on function public.get_graph_membros(bigint, boolean, int) to svc_kg;
# grant select on public.membros, public.funcoes, public.faccoes to svc_kg;
# (Opcional) proteja a API com header x-api-key (se ativar no app.py)
# API_KEY=uma_chave_bem_forte
# Supabase (na sua VPS / Coolify)
SUPABASE_URL=https://supabase.mondaha.com
SUPABASE_KEY=SEU_SUPABASE_SERVICE_ROLE_OU_ANON

# Nome da função RPC
SUPABASE_RPC_FUNCTION=get_graph_membros

# Cache do navegador/CDN
HTTP_CACHE_SECONDS=30
HTTP_STALE_WHILE_REVALIDATE_SECONDS=120

# Cache em memória do servidor
CACHE_TTL_SECONDS=60
CACHE_MAX_ITEMS=512

# CORS (separado por vírgula). Use * para liberar tudo (não recomendado p/ produção)
CORS_ALLOW_ORIGINS=https://app.flutterflow.io,https://*.flutterflow.app,https://*.vercel.app


